# react-async-loader-example
example of implementing async loading using react

Usage exmaple: 



```
// in some render method of a react component
render(){
  return(
    <LoadAsyncCompFunc componentFunc={Tables} />
  )
}
```
